# Secret-leak findings — surface scan

Scope: `.env*`, `env.local*`, and source files in the 13 target sibling projects. Scanned 2026-05-17.

All literal values below are redacted. The **prefix / context** is shown so the project owner can locate and rotate.

## URGENT — committed real secrets, plaintext on disk

### 1. `mas-site/env.local` and `Final Store and site/matrix-advanced-solutions/env.local`

**Same Supabase project, same keys, in two places.**

- `mas-site/env.local:6` — `NEXT_PUBLIC_SUPABASE_URL=https://iszlhbzamiqnkprlulxc.supabase.co`
- `mas-site/env.local:7` — `NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJI...` (full anon JWT present)
- `mas-site/env.local:8` — `SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJI...` (**full service-role JWT present — full DB access, bypasses RLS**)
- `mas-site/env.local:11-24` — 5 additional keys (likely Stripe/Resend/GHL/etc., values redacted by length)
- `Final Store and site/matrix-advanced-solutions/env.local:6-24` — **identical** to mas-site (same project ref `iszlhbzamiqnkprlulxc`, identical anon + service-role tokens)

These files are **not** in `.gitignore` of either repo (filename is `env.local` not `.env.local`, so the standard Next.js `.gitignore` `.env*.local` pattern misses them). They are likely already committed.

**Action:**
1. Rotate Supabase anon + service-role keys in project `iszlhbzamiqnkprlulxc` immediately.
2. Rename both files to `.env.local` (leading dot) and add to `.gitignore`.
3. `git filter-repo` / BFG to scrub history.

### 2. `Final Store and site/for-all-health/.env.local`

- Line 9 — `NEXT_PUBLIC_SUPABASE_URL=https://xgdglafefiebboxiuesl.supabase.co` (URL exposed)
- Line 10 — `NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_p...` (publishable, real value present)
- Line 11 — `SUPABASE_PROJECT_REF=xgdg...` (real ref)
- Lines 16, 19–28 — `SUPABASE_SERVICE_ROLE_KEY`, Stripe, Resend, CRON_SECRET — **all empty**

Lower severity than #1 — service-role is empty. But the publishable key + URL are committed and the file is named `.env.local` (so it likely *is* gitignored). Verify `.gitignore`.

**Action:** confirm `.env.local` is gitignored in `for-all-health/`. Rotate the publishable Supabase key if the repo is public.

### 3. `Tipsy Trivia/.env`

- Line 9 — single `=<value>` (real secret, prefix not visible to grep — likely DB or API token)
- Line 24 — single `=<value>` (real secret)

File is named `.env` (no leading-dot suffix). Repo has a `.gitignore` (79 B) — verify it covers `.env`. Otherwise this is committed.

**Action:** inspect `Tipsy Trivia/.gitignore`. Rotate if committed.

### 4. `Tipsy Trivia/.env.vercel`, `.env.vercel.prod`, `.vercel/.env.development.local`

Each contains a **Vercel OIDC token** (RS256 JWT, line 2 / line 20 / line 2 respectively).

- Token issuer: `https://oidc.vercel.com/prosalesleague-8964s-projects`
- Subject: `owner:prosalesleague-8964s-projects:project:tipsy-trivia:environment:development`
- Project ID: `prj_8bf82jRg3QGbLIHEoeTHQGi4yDEM`
- Team ID: `team_AkNfDpUG9zfbTrC7xYFZfjNo`
- Expiries vary (one is `iat 1772941196 / exp 1772984396` — short-lived, but the file itself persists across rotations).

These tokens are short-lived BUT the files are regenerated by `vercel pull` and tend to get committed accidentally. Confirm `.vercel/` and `*.env.vercel*` are gitignored.

**Action:** add `.vercel/` and `.env.vercel*` to `Tipsy Trivia/.gitignore`. Tokens themselves expire; no rotation needed.

## Low-risk / informational

### `mas-site` and `matrix-advanced-solutions` env files appear identical

Two copies of the same secrets on disk — single rotation fixes both, but increases blast radius if either repo is pushed.

### Supabase project `iszlhbzamiqnkprlulxc`

Service-role JWT decoded: `iss=supabase`, `ref=iszlhbzamiqnkprlulxc`, `role=service_role`, `iat=1772430995`, `exp=2088006995` (expires year 2036 — effectively non-expiring).

This single token currently grants god-mode access to the project's database. **Highest-priority rotation.**

## Found but benign

- `AI Company/Code & Tooling/ghl-builder-cli/.env.example`, `AI Company/Code & Tooling/notetaker-main/.env.example`, `AI Company/Skill Packs/everything-claude-code-main/.env.example`, `Matrix Academy/lms/.env.local.example`, `Final Store and site/b2b-emr copy/.env.local.example`, `Final Store and site/b2b-emr/.env.local.example`, `Final Store and site/matrix-advanced-solutions/env.local.example`, `Final Store and site/for-all-health/env.local.example`, `mas-site/env.local.example`, `Tipsy Trivia/.env.example`, `Gentle Reminder/services/api/.env.example` — all `*.example` placeholders, safe.

## Source-code scan

Grep across `*.js *.ts *.tsx *.py *.json` for `sk-`, `sk_live`, `sk_test`, `AKIA`, `ghp_`, `xoxb` in the 13 projects returned **no hits** outside of `node_modules` (which is expected to contain those prefixes in fixtures/test files and was excluded). No hard-coded keys in app source on this scan.

## Summary

| Severity | Count | Where |
|---|---|---|
| URGENT (real prod creds, may be committed) | 1 Supabase service-role JWT (duplicated in 2 files) | `mas-site/env.local`, `matrix-advanced-solutions/env.local` |
| HIGH (real anon/publishable creds) | 2 Supabase anon keys, 1 publishable | same files + `for-all-health/.env.local` |
| HIGH (real third-party tokens, prefix unknown) | 2 lines in `Tipsy Trivia/.env` | `Tipsy Trivia/.env` |
| LOW (short-lived Vercel OIDC) | 3 JWTs | `Tipsy Trivia/.env.vercel*`, `.vercel/.env.development.local` |
| BENIGN | 11 `.example` files | various |

**Top action:** rotate the `iszlhbzamiqnkprlulxc` Supabase service-role key today, then rename `env.local` → `.env.local` in both `mas-site/` and `matrix-advanced-solutions/`, then scrub git history.
